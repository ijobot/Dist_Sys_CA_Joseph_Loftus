syntax = "proto3";

package security;

service SecurityService{
    rpc InitializeService(SecurityMessage) returns (SecurityResponse);
}

message SecurityMessage{
    string user = 1;     
    string message = 2;
}

message SecurityResponse{
    string response = 1;     
}