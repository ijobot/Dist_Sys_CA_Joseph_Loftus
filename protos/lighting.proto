syntax = "proto3";

package lighting;

service LightingService{
    rpc InitializeService(LightingMessage) returns (LightingResponse);
}

message LightingMessage{
    string user = 1;     
    string message = 2;   
}

message LightingResponse{
    string response = 1;     
}
